1.创建组件的原理：一个组件只做一件事情，如果组件的的功能在逐渐增多，它应该被分割为更小的子组件
2.不要使用state去创建静态版本的应用程序，state只在交互性功能上使用。在创建静态版本的应用程序和渲染
数据模型，使用props从父组件到子组件传递数据的方式。
3.在传统的JavaScript中，为了this的值是正确的，通常需要手动的绑定一个方法到它的实例中，在react中每一个方法
被自动的绑定到它的组件实例中。因此在react组件中的每个函数中使用this，这个this都表示当前组件实例